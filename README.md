# SAMMI Next
<!-- TODO: no se te vaya a olvidar hacer este README.md hdtpm (de hecho, todos los README.md) -->
To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.3.5. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
