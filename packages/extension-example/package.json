{
    "name": "extension-example",
    "type": "module",
    "dependencies": {
        "sammi-next": "workspace:sammi-next",
        "sammi-bridge-types": "workspace:sammi-bridge-types"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "chokidar": "^5.0.0",
        "esbuild": "^0.27.2",
        "minimist": "^1.2.8",
        "typescript": "^5"
    },
    "scripts": {
        "build": "node build.cjs",
        "watch": "node build.cjs --watch",
        "dev": "node build.cjs --watch --dev",
        "build:bun": "bun run build.ts"
    }
}